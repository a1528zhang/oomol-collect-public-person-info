version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    pip install -r requirements.txt
icon: ":twemoji:detective:currentColor:"
name: collect-public-person-info
description: Collect public info by username.
displayName: Collect Public Person Info
repository: https://github.com/a1528zhang/oomol-collect-public-person-info
